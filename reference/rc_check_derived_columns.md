# Checks on values calculated in the EDC software Check target sum at any time, at baseline, and at Nadir In TrialMaster, if `target_sum` is modified, `target_sum_bl` & `target_sum_min` are not updated automatically. This also makes duplicates possible.

Checks on values calculated in the EDC software Check target sum at any
time, at baseline, and at Nadir In TrialMaster, if `target_sum` is
modified, `target_sum_bl` & `target_sum_min` are not updated
automatically. This also makes duplicates possible.

## Usage

``` r
rc_check_derived_columns(rc)
```
